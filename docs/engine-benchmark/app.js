import {DRILL_STANDARD_PRESET, OPTION_METADATA} from './options.js';
const $=id=>document.getElementById(id), workers=new Map(); let engines=[];
const yakuNames=['門前清自摸和','立直','一発','断么九','平和','一盃口','槍槓','嶺上開花','海底摸月','河底撈魚','ドラ','裏ドラ','赤ドラ','白','發','中'];
function tiles(text){const o=[],b={m:0,p:9,s:18,z:27};let d='';for(const c of text.replace(/\s/g,'')){if(/\d/.test(c))d+=c;else if(b[c]!==undefined){for(const x of d)o.push(b[c]+(x==='0'?4:+x-1));d=''}else throw Error(`不正な牌表記: ${c}`)}if(d)throw Error('牌種 m/p/s/z が不足しています');return o}
function value(m){const e=$(`opt-${m.key}`);if(m.type==='boolean')return e.checked;if(m.type==='json')return JSON.parse(e.value);if(m.type==='text')return e.value;return m.auto&&e.value===''?Math.max(0,(18-+$('turn').value)*4):+e.value}
function set(m,v){const e=$(`opt-${m.key}`);if(!e)return;e[m.type==='boolean'?'checked':'value']=m.type==='json'?JSON.stringify(v):v}
function renderOptions(){const groups={};for(const m of OPTION_METADATA)if(!m.scene)(groups[m.category]??=[]).push(m);for(const [group,items]of Object.entries(groups)){const fs=document.createElement('fieldset');fs.innerHTML=`<legend>${group}</legend>`;for(const m of items){const l=document.createElement('label');l.className='option';const c=m.type==='boolean'?`<input id="opt-${m.key}" type="checkbox">`:m.type==='json'?`<textarea id="opt-${m.key}" rows="2"></textarea>`:m.type==='text'?`<input id="opt-${m.key}" type="text">`:`<input id="opt-${m.key}" type="number" ${m.min!==undefined?`min="${m.min}"`:''} ${m.max!==undefined?`max="${m.max}"`:''} ${m.step!==undefined?`step="${m.step}"`:''}>`;l.innerHTML=`<span><strong>${m.label}</strong> <code>${m.key}</code><small>${m.description}</small></span>${c}`;fs.append(l)}$('advanced-options').append(fs)}for(const m of OPTION_METADATA.filter(x=>!x.scene))$(`opt-${m.key}`).addEventListener('input',()=>{$('mode-advanced').checked=true;sync();diff()})}
function settings(){const o={};for(const m of OPTION_METADATA)if(!m.scene)o[m.key]=value(m);o.t_min=+$('turn').value;o.remaining_tiles=value(OPTION_METADATA.find(m=>m.key==='remaining_tiles'));return o}
function selected(){return [$('engineA').value,$('engineB').value].map(id=>engines.find(e=>e.id===id))}
function standard(){for(const m of OPTION_METADATA)if(!m.scene&&m.key in DRILL_STANDARD_PRESET)set(m,DRILL_STANDARD_PRESET[m.key]);$('mode-standard').checked=true;sync();diff()}
function sync(){const s=settings(),[a,b]=selected(),fast=s.calc_exp_score_only,both=[a,b].every(e=>e?.capabilities?.calc_exp_score_only),any=[a,b].some(e=>e?.capabilities?.calc_exp_score_only);for(const k of ['calc_stats','calc_yaku_stats','calc_shapley_stats']){const e=$(`opt-${k}`);e.disabled=fast;if(fast)e.checked=false}const fastEl=$('opt-calc_exp_score_only');fastEl.disabled=!any;if(fast&&!any)fastEl.checked=false;$('capability-note').textContent=fast&&!both?'EV-only は片方のengineのみ対応です。対応engineの単独実行は可能ですが、比較実行はできません。':fast?'EV-only: 統計・役別Shapleyは送信されません。':'';$('capA').textContent=`${a?.label}: EV-only ${a?.capabilities?.calc_exp_score_only?'対応':'非対応'}`;$('capB').textContent=`${b?.label}: EV-only ${b?.capabilities?.calc_exp_score_only?'対応':'非対応'}`}
function diff(){const s=settings(),d=OPTION_METADATA.filter(m=>!m.scene&&m.key in DRILL_STANDARD_PRESET&&JSON.stringify(s[m.key])!==JSON.stringify(DRILL_STANDARD_PRESET[m.key]));$('preset-diff').textContent=d.length?`標準設定から ${d.length} 項目変更: ${d.map(m=>m.key).join(', ')}`:'何切るドリル標準設定と一致しています。'}
function request(engine){const s=settings(),p={game_mode:1,round_wind:+$('round').value,seat_wind:+$('seat').value,dora_indicators:tiles($('dora').value),hand:tiles($('hand').value),melds:JSON.parse($('melds').value),version:engine.version,...s};if(s.yaku_filter==='')delete p.yaku_filter;else p.yaku_filter=Number(s.yaku_filter);if(s.calc_exp_score_only)for(const k of ['calc_stats','calc_yaku_stats','calc_shapley_stats'])delete p[k];return p}
function worker(engine){if(workers.has(engine.id))return workers.get(engine.id);const w=new Worker(`${engine.path}worker.js`,{type:'module'});let id=0;const q=new Map();w.onmessage=e=>{const p=q.get(e.data.id);if(!p)return;q.delete(e.data.id);e.data.error?p.reject(Error(e.data.error)):p.resolve(e.data)};const api={run:p=>new Promise((resolve,reject)=>{const n=++id;q.set(n,{resolve,reject});w.postMessage({id:n,payload:p})})};workers.set(engine.id,api);return api}
async function run(engine){const p=request(engine),w=worker(engine),l=performance.now();$('status').textContent=`${engine.label} を warm-up / 実行中…`;await w.run({...p,t_min:1,t_max:1,remaining_tiles:68,extra:0,calc_exp_score_only:true});const loadMs=performance.now()-l,t=performance.now(),out=await w.run(p);return {engine,response:out.result,request:p,loadMs,calcMs:performance.now()-t}}
function best(r){if(!r?.success)throw Error(r?.err_msg||'engine response is malformed');const t=r.config.t_min;return r.stats.reduce((a,x)=>!a||x.exp_score[t]>a.exp_score[t]?x:a)} const num=x=>Number.isFinite(x)?x:0;
function summary(a,b){const A=best(a.response),B=best(b.response),t=a.response.config.t_min,p=x=>x.response.profile||{},e=x=>p(x).edges;return `<table><tr><th></th><th>${a.engine.label}</th><th>${b.engine.label}</th><th>差分</th></tr><tr><th>推奨牌</th><td>${A.tile}</td><td>${B.tile}</td><td>${A.tile===B.tile?'一致':'不一致'}</td></tr><tr><th>期待値</th><td>${A.exp_score[t].toFixed(6)}</td><td>${B.exp_score[t].toFixed(6)}</td><td>${Math.abs(A.exp_score[t]-B.exp_score[t]).toExponential(3)}</td></tr><tr><th>和了率</th><td>${(num(A.win_prob?.[t])*100).toFixed(4)}%</td><td>${(num(B.win_prob?.[t])*100).toFixed(4)}%</td><td>${((num(B.win_prob?.[t])-num(A.win_prob?.[t]))*100).toExponential(3)}</td></tr><tr><th>聴牌率</th><td>${(num(A.tenpai_prob?.[t])*100).toFixed(4)}%</td><td>${(num(B.tenpai_prob?.[t])*100).toFixed(4)}%</td><td>${((num(B.tenpai_prob?.[t])-num(A.tenpai_prob?.[t]))*100).toExponential(3)}</td></tr><tr><th>計算時間 (warm)</th><td>${a.calcMs.toFixed(2)} ms</td><td>${b.calcMs.toFixed(2)} ms</td><td>${(a.calcMs/b.calcMs).toFixed(2)}x</td></tr><tr><th>初回 load + warm-up</th><td>${a.loadMs.toFixed(2)} ms</td><td>${b.loadMs.toFixed(2)} ms</td><td>—</td></tr><tr><th>states / edges</th><td>${a.response.searched} / ${e(a)??'—'}</td><td>${b.response.searched} / ${e(b)??'—'}</td><td>${e(a)==null||e(b)==null?'—':e(b)-e(a)}</td></tr><tr><th>graph / DP</th><td>${p(a).graph_build_us??'—'} / ${p(a).dp_us??'—'} μs</td><td>${p(b).graph_build_us??'—'} / ${p(b).dp_us??'—'} μs</td><td>—</td></tr></table>`}
function yaku(s,t){return(s.yaku_stats||[]).map((y,i)=>`<tr><td>${yakuNames[i]||`役 ${y.yaku}`}</td><td>${num(y.shapley_score?.[t]).toFixed(3)}</td><td>${(num(y.occurrence_prob?.[t])*100).toFixed(3)}%</td></tr>`).join('')||'<tr><td colspan="3">役別統計なし</td></tr>'}
function details(a,b){const t=a.response.config.t_min,am=new Map(a.response.stats.map(s=>[s.tile,s])),bm=new Map(b.response.stats.map(s=>[s.tile,s])),ts=[...new Set([...am.keys(),...bm.keys()])].sort((x,y)=>(bm.get(y)?.exp_score[t]??-Infinity)-(bm.get(x)?.exp_score[t]??-Infinity));return `<h2>候補打牌ごとの役別Shapley</h2>${ts.map(tile=>{const A=am.get(tile),B=bm.get(tile);return `<details><summary>打牌 ${tile} — EV: ${A?.exp_score[t]?.toFixed(3)??'—'} / ${B?.exp_score[t]?.toFixed(3)??'—'}</summary><div class="yaku-grid"><table><caption>${a.engine.label}</caption><tr><th>役</th><th>Shapley EV</th><th>出現率</th></tr>${A?yaku(A,t):''}</table><table><caption>${b.engine.label}</caption><tr><th>役</th><th>Shapley EV</th><th>出現率</th></tr>${B?yaku(B,t):''}</table></div></details>`}).join('')}`}
function show(a,b){$('result').innerHTML=summary(a,b)+details(a,b);$('request-json').textContent=JSON.stringify({engineA:a.request,engineB:b.request},null,2)}
async function execute(kind){try{const[a,b]=selected(),s=settings(),target=kind==='a'?a:kind==='b'?b:null;if(s.calc_exp_score_only&&((target&&!target.capabilities.calc_exp_score_only)||(kind==='both'&&(!a.capabilities.calc_exp_score_only||!b.capabilities.calc_exp_score_only))))throw Error('EV-only はこのengine組合せで同条件に実行できません。');const r=kind==='a'?await run(a):kind==='b'?await run(b):null;if(r)show(r,r);else{const[ra,rb]=await Promise.all([run(a),run(b)]);show(ra,rb)}$('status').textContent='完了。計算時間は warm-up 後の WASM 実行時間です。'}catch(e){$('status').textContent=`エラー: ${e.message}`}}
async function init(){renderOptions();engines=await fetch('engines.json').then(r=>r.json());for(const id of ['engineA','engineB'])$(id).innerHTML=engines.map(e=>`<option value="${e.id}">${e.label}</option>`).join('');$('engineB').value='candidate';standard();$('mode-standard').onchange=standard;$('mode-advanced').onchange=()=>{sync();diff()};$('reset-standard').onclick=standard;$('engineA').onchange=sync;$('engineB').onchange=sync;$('turn').oninput=diff;$('runA').onclick=()=>execute('a');$('runB').onclick=()=>execute('b');$('compare').onclick=()=>execute('both');$('show-request').onclick=()=>$('request-detail').open=true}
init();
